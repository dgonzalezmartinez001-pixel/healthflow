name: 'healthflow'
version: '1.0.0'
config-version: 2

profile: 'healthflow'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  healthflow:
    staging:
      +schema: staging
      +materialized: view
    intermediate:
      +schema: intermediate
      +materialized: table
    marts:
      +schema: marts
      +materialized: table
    gold:
      +schema: gold
      +materialized: table